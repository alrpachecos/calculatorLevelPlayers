import calculatorRankedMatch from "./utils/calculatorRankedMatch.js";

let resultRanked = calculatorRankedMatch(100, 10);
console.log(`O Herói tem saldo de ${resultRanked.result} e está no nível ${resultRanked.levelOfPlayer}.\n`);